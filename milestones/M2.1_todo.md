# M2.1 — TDD 체크리스트

순서대로 진행. 각 단위는 **테스트 먼저 → 구현 → 통과 → 단위 커밋** 사이클. 자세한 의도·구조는 [`M2.1_plan.md`](./M2.1_plan.md).

## 1. Config 확장 (§3.1)

- [ ] `tests/test_config_m2_1.py` 작성 (4 케이스)
  - `test_default_parallel_is_two`
  - `test_default_concurrency_is_three`
  - `test_parallel_clamps_zero_and_negative_to_one`
  - `test_toml_roundtrip_preserves_new_field`
- [ ] `pytest tests/test_config_m2_1.py -v` → 4 fail 확인 (red)
- [ ] `src/gah/config.py` 수정
  - `analysis_concurrency` default `1 → 3`
  - `ollama_parallel: int = 2` 신규
  - `from_mapping` 에서 `ollama_parallel` 음수/0 → 1 클램프
- [ ] 기존 `tests/test_config_m2.py` 의 `analysis_concurrency=1` 가정이 있으면 갱신
- [ ] `pytest tests/test_config_m2_1.py tests/test_config_m2.py tests/test_config.py -v` 통과 (green)

## 2. OllamaClient 세마포어 (§3.2)

- [ ] `tests/test_ollama_client_concurrency.py` 작성 (3 케이스)
  - `test_chat_caps_concurrent_calls_at_parallel`
  - `test_embed_shares_semaphore_with_chat`
  - `test_parallel_one_serializes_completely`
  - 핵심 기법: respx 핸들러에 sleep + threading.Lock 으로 in-flight 카운터 max 관측
- [ ] red 확인
- [ ] `src/gah/core/ollama_client.py` 수정
  - `__init__(..., parallel: int = 2)` 추가
  - `self.parallel = max(1, int(parallel))`, `self._sem = threading.Semaphore(self.parallel)`
  - `chat()` / `embed()` 외부 호출 본문을 `with self._sem:` 로 감싸기
  - retry/sleep 도 세마포어 안에서
- [ ] 기존 `tests/test_ollama_client.py` 전부 green 유지
- [ ] 새 동시성 테스트 50회 반복 flake-free 확인

## 3. ClipLabeler 직렬화 락 (§3.3)

- [ ] `tests/test_clip_labeler_concurrency.py` 작성 (2 케이스)
  - `test_score_image_serialized_across_threads`
  - `test_warmup_serialized_across_threads`
  - 핵심: `FakeBackend` 에 `concurrent_calls` 카운터 추가(이미 있는 `encode_image_call_count` 와 별개), 진입 시 +1 / 종료 시 -1, 진입 시 ≤1 invariant
- [ ] red 확인
- [ ] `src/gah/core/clip_labeler.py` 수정
  - `ClipLabeler.__init__` 에 `self._lock = threading.Lock()`
  - `warmup()` / `score_image()` 본문을 `with self._lock:` 감싸기 (early return `if not self.enabled` 는 lock 밖)
- [ ] 기존 `tests/test_clip_labeler.py` green 유지
- [ ] 새 테스트 50회 반복 flake-free

## 4. Store write lock + busy_timeout + mark_asset_pending (§3.4)

- [ ] `tests/test_store_concurrency.py` 작성 (3 케이스)
  - `test_write_lock_prevents_database_is_locked` — 2 thread × 100 회 mixed write
  - `test_mark_asset_pending_updates_state`
  - `test_busy_timeout_pragma_set` — `PRAGMA busy_timeout` 조회로 5000 확인
- [ ] red 확인
- [ ] `src/gah/core/store.py` 수정
  - `__init__` 끝에 `self.conn.execute("PRAGMA busy_timeout = 5000")`
  - `self.write_lock = threading.RLock()`
  - 15 개 writer 메서드 본문을 `with self.write_lock:` 으로 감싸기 (목록은 plan §3.4)
  - 신규 메서드 `mark_asset_pending(asset_id)` — UPDATE state='pending'
- [ ] 기존 `tests/test_store.py` + `tests/test_store_m2.py` green 유지
- [ ] 새 동시성 테스트 50회 반복 flake-free

## 5. LabelRegistry 락 (§3.5)

- [ ] `src/gah/core/labels.py` 수정
  - `bootstrap()` 의 BEGIN/loop/COMMIT 구간을 `with self.store.write_lock:` 안에서
  - `add_label()` 의 INSERT/UPDATE 구간을 lock 안에서
  - `set_enabled()` UPDATE 를 lock 안에서
  - `set_description()` UPDATE 를 lock 안에서
- [ ] `tests/test_labels.py` green 유지 (별도 신규 테스트 없음)

## 6. analysis_queue 정리 (§3.6)

- [ ] `src/gah/core/analysis_queue.py` 수정
  - `drain_pending()` 의 raw `self.store.conn.execute("UPDATE assets SET analysis_state='pending' ...")` 를 `self.store.mark_asset_pending(row.id)` 로 교체
- [ ] `tests/test_analysis_queue.py` green 유지

## 7. app.py 와이어업 (§3.7)

- [ ] `src/gah/app.py` 수정
  - `OllamaClient(...)` 에 `parallel=config.ollama_parallel` 인자 추가
- [ ] `pytest -q` 부분 통과 확인(이 단계까지 본 후 §8 로)

## 8. MainWindow update_progress 디바운스 (§3.8)

- [ ] `tests/test_progress_debounce.py` 작성 (3 케이스)
  - `test_first_update_renders_immediately`
  - `test_rapid_updates_collapse_to_at_most_two_renders_per_window`
  - `test_final_snapshot_eventually_rendered_after_window`
  - 기법: `MainWindow._flush_progress` 호출 카운터 + `QTimer` + `QEventLoop.processEvents()` (또는 `QTest.qWait(...)`)
- [ ] red 확인
- [ ] `src/gah/ui/main_window.py` 수정
  - `__init__` 에 `self._pending_snapshot = None`, `self._progress_flush_timer = None`
  - `update_progress(snapshot)` → snapshot 저장 + 타이머 없으면 즉시 flush + 250ms 타이머 예약
  - `_on_progress_window_end()` → flush + 타이머 해제
  - `_flush_progress()` → 기존 setRange/setValue/setText 본문
- [ ] 기존 `tests/test_progress_statusbar.py` 통과 확인(leading-edge 즉시 그리기 유지)

## 9. (선택) AnalysisQueue concurrency smoke (§4)

- [ ] `tests/test_analysis_queue_m2_1.py` 작성 (1 케이스)
  - `test_concurrency_three_runs_workers_in_parallel`
  - 기법: fake analyzer 가 `threading.Event` 로 동시 진입 관측
- [ ] 통과

## 10. 회귀

- [ ] `pytest -q` 전체 실행 → 모두 통과
- [ ] 신규 동시성 테스트 5종을 5회 반복 실행해 flake 없음 확인
  ```powershell
  pytest tests/test_ollama_client_concurrency.py tests/test_clip_labeler_concurrency.py tests/test_store_concurrency.py tests/test_progress_debounce.py -v --count=5
  ```
  (또는 `for i in 1..5 { pytest ... }`. pytest-repeat 가 없으면 그냥 5회 반복 실행)

## 11. 문서

- [ ] `milestones/M2.1_verification.md` 작성 (자동 결과 + 사용자 수동 검증 7단계)
- [ ] `milestones/M2_verification.md` §1 카운트 + §3 수동 검증에 동시성 항목 한두 줄 추가
- [ ] `HANDOFF.md` 의 §1·§2·§3 갱신
  - §1: M2.1 통과를 반영한 한 줄 요약
  - §2: pytest 합계 갱신
  - "다음 작업" 은 여전히 M3
